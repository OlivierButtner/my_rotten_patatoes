/*
import axios from "axios";
import {useRouter} from 'next/router';

export default function DeleteComment(id){

    const router = useRouter();



    function deleteUs () {
        axios.delete(`http://localhost:3000/api/movies/${id.id}`)
        router.push('/Admin')
    }
    return (
        <div>
            <button onClick={deleteUs}>
                Delete Movie
            </button>
        </div>
    );
}
*/
